<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="detail">
	<select id="selectaddAcademy" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
		INSERT INTO DETAIL
		(MB_NICK, DTL_SUMM, DTL_INFO, DTL_URL, DTL_PHONE)
		VALUES
		(#{MB_NICK}, #{DTL_SUMM}, #{DTL_INFO}, #{DTL_URL}, #{DTL_PHONE}) 
	]]>
	</select>
</mapper>




